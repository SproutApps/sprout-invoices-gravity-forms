;(function( $, si, undefined ) {
    
    si.gravityForms = {
        config: {
        },
    };

    /**
     * methods
     */
    si.gravityForms.init = function() {
        // stuff
        console.log( 'loaded' );
    };
    
})( jQuery, window.si = window.si || {} );

// Init
jQuery(function() {
    si.gravityForms.init();
});
